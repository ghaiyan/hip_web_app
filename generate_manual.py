"""
生成髋关节关键点检测与参数计算系统——说明书.docx
参考模板结构: 引言 → 概述 → 软件功能及使用说明
"""
from docx import Document
from docx.shared import Pt, Inches, Cm, RGBColor, Emu
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT
from docx.oxml.ns import qn
from docx.oxml import OxmlElement
import os

OUTPUT_PATH = r"C:\Users\mfqus\WorkBuddy\2026-06-02-15-33-23\hip_web_app\髋关节关键点检测与参数计算系统——说明书.docx"

doc = Document()

# ============================================================
#  样式设置
# ============================================================
style = doc.styles['Normal']
font = style.font
font.name = '宋体'
font.size = Pt(12)
style.element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')
style.paragraph_format.line_spacing = 1.5
style.paragraph_format.first_line_indent = Cm(0.74)

# 标题样式
for level in [1, 2]:
    h_style = doc.styles[f'Heading {level}']
    h_font = h_style.font
    h_font.name = '黑体'
    h_style.element.rPr.rFonts.set(qn('w:eastAsia'), '黑体')
    h_font.color.rgb = RGBColor(0, 0, 0)
    if level == 1:
        h_font.size = Pt(16)
        h_font.bold = True
    else:
        h_font.size = Pt(14)
        h_font.bold = True
    h_style.paragraph_format.first_line_indent = Cm(0)
    h_style.paragraph_format.space_before = Pt(12)
    h_style.paragraph_format.space_after = Pt(6)

def add_para(text, bold=False, align=None, indent=True, font_size=12, font_name='宋体'):
    """添加正文段落"""
    p = doc.add_paragraph()
    if not indent:
        p.paragraph_format.first_line_indent = Cm(0)
    if align:
        p.alignment = align
    run = p.add_run(text)
    run.font.size = Pt(font_size)
    run.font.name = font_name
    run._element.rPr.rFonts.set(qn('w:eastAsia'), font_name)
    run.bold = bold
    return p

def add_heading_custom(text, level=1):
    """添加标题"""
    h = doc.add_heading(text, level=level)
    for run in h.runs:
        run.font.name = '黑体'
        run._element.rPr.rFonts.set(qn('w:eastAsia'), '黑体')
        run.font.color.rgb = RGBColor(0, 0, 0)
        if level == 1:
            run.font.size = Pt(16)
        else:
            run.font.size = Pt(14)
    return h

# ============================================================
#  封面标题
# ============================================================
for _ in range(4):
    doc.add_paragraph()

title_p = doc.add_paragraph()
title_p.alignment = WD_ALIGN_PARAGRAPH.CENTER
title_run = title_p.add_run('髋关节关键点检测与参数计算系统说明书')
title_run.font.size = Pt(22)
title_run.font.name = '黑体'
title_run._element.rPr.rFonts.set(qn('w:eastAsia'), '黑体')
title_run.bold = True

doc.add_paragraph()
doc.add_paragraph()

# 分页
doc.add_page_break()

# ============================================================
#  1 引言
# ============================================================
add_heading_custom('1 引言', 1)

add_heading_custom('1.1 软件用途', 2)

add_para(
    '本软件主要用于髋关节X射线正位影像的关键点自动检测与临床参数计算，'
    '面向骨科临床、运动医学及医学影像分析领域的研究人员和临床医生。'
    '通过集成深度学习关键点检测模型与临床参数自动计算功能，软件可辅助用户快速、准确地'
    '从髋关节X射线正位片中定位19个解剖关键点，并自动计算外展角、前倾角、颈干角、'
    '股骨偏心距及下肢长度差等五项关键临床参数。'
)

add_para(
    '软件支持通过Web界面上传髋关节X射线正位影像，自动完成图像预处理、模型推理、'
    '关键点可视化标注及参数计算全流程，并提供中英文双语界面切换功能，'
    '广泛适用于医院的骨科门诊、影像科辅助诊断、临床科研数据分析以及'
    '医学院校的教学培训等应用场景。系统可有效降低人工测量带来的主观误差，'
    '提高髋关节参数测量的标准化程度和工作效率，为全髋关节置换术的术前规划和'
    '术后评估提供客观、可重复的量化依据。'
)

add_heading_custom('1.2 软件运行环境', 2)

add_para(
    '本系统的开发基于Windows操作系统，采用VSCode作为集成开发环境，'
    '构建于Python编程语言与FastAPI Web框架之上。系统后端核心功能模块使用Python编写，'
    '集成了PyTorch深度学习框架用于DRFNet关键点检测模型的加载与前向推理计算，'
    '同时利用Pillow图像处理库、NumPy科学计算库等实现图像预处理、坐标变换与可视化标注等任务。'
)

add_para(
    '前端界面采用原生HTML/CSS/JavaScript构建，无需额外安装前端框架依赖，'
    '通过Fetch API与后端FastAPI服务进行异步数据交互，实现上传进度反馈、'
    '检测结果展示及参数计算报告的动态渲染。系统通过uvicorn作为ASGI服务器'
    '提供Web服务，采用RESTful API设计实现前后端解耦。'
    '模型加载采用延迟初始化策略，在首次检测请求时自动加载权重文件，'
    '支持GPU加速推理（CUDA）与CPU回退模式，兼顾计算效率与部署灵活性。'
    '各功能模块间通过统一的JSON数据协议进行通信，确保系统的可扩展性与可维护性。'
)

# ============================================================
#  2 概述
# ============================================================
add_heading_custom('2 概述', 1)

add_heading_custom('2.1 系统概述', 2)

add_para(
    '本软件是基于Python编程语言，结合FastAPI Web框架开发的全栈Web应用系统，'
    '主要实现了影像上传与管理模块、关键点智能检测模块、检测结果可视化模块、'
    '临床参数自动计算模块以及多语言界面切换模块等核心功能。'
)

add_para(
    '其中影像上传与管理模块支持用户通过拖拽或点击选择的方式上传髋关节X射线'
    '正位影像图片（支持JPG、PNG、BMP等常见格式），系统自动完成图像尺寸归一化'
    '（512×512像素）及色彩空间转换等预处理操作，并通过进度条提供上传状态反馈。'
)

add_para(
    '关键点智能检测模块基于DRFNet深度学习模型架构，采用ResNet50骨干网络提取'
    '多层次图像特征，结合PAN-FPN特征金字塔网络实现多尺度特征融合，最终通过'
    '热力图解码器输出19个解剖关键点的高斯热力图预测，经峰值检测算法提取'
    '亚像素精度的关键点坐标。该19个关键点覆盖骨盆基准点（泪滴点及耻骨联合）、'
    '双侧股骨头中心、股骨颈中心、股骨干近端及远端标志点、以及双侧臼杯外上缘、'
    '内下缘、前后缘等关键解剖位置。'
)

add_para(
    '检测结果可视化模块利用Pillow图像处理库将检测到的关键点以彩色标记点的形式'
    '叠加显示在原始X射线影像上，不同解剖组别的关键点采用不同颜色标识（骨盆基准为黄色、'
    '左侧结构为橙/红色系、右侧结构为暖/青色系），每个关键点附带编号标签，'
    '并绘制关键解剖连线（如泪滴连线、股骨干轴线、臼杯椭圆拟合线等），'
    '便于用户直观判断检测结果的准确性。'
)

add_para(
    '临床参数自动计算模块基于检测到的关键点坐标，利用空间几何与解剖学关系建模，'
    '自动计算五项临床参数：（1）臼杯外展角——基于臼杯椭圆长轴与骨盆基准线夹角；'
    '（2）臼杯前倾角——基于臼杯椭圆短轴与长轴之比的反三角函数；'
    '（3）颈干角——基于股骨头中心-股骨颈中心连线与股骨干轴线的夹角；'
    '（4）股骨偏心距——基于股骨头中心到股骨干轴线的垂直距离；'
    '（5）下肢长度差——基于双侧小转子或坐骨结节连线的高度差。'
    '每项参数均附带临床参考范围，当计算值超出正常范围时自动触发警示提示。'
)

add_para(
    '多语言界面切换模块提供中文和英文两种显示语言，支持一键切换，所有界面文字、'
    '按钮标签、参数名称、警示信息及帮助提示均实现双语覆盖。系统整体采用模块化设计，'
    '前后端职责明确，接口规范统一，具备良好的可扩展性与交互性，'
    '适用于骨科临床辅助诊断、髋关节手术术前规划评估、医学科研数据分析及'
    '医学影像教学培训等多种场景。'
)

add_heading_custom('2.2 系统架构', 2)

add_para(
    '本系统采用前后端分离的B/S架构设计，整体由前端展示层、后端服务层和模型推理层'
    '三个核心层次组成。前端展示层负责用户交互与结果渲染，后端服务层负责请求路由、'
    '数据预处理和业务逻辑协调，模型推理层负责深度学习模型的加载与关键点预测计算。'
    '三层次之间通过HTTP协议和JSON数据格式实现松耦合通信。系统的整体架构如图2-2-1所示。',
    indent=False
)

add_para('')
add_para('图2-2-1 髋关节关键点检测与参数计算系统架构图', align=WD_ALIGN_PARAGRAPH.CENTER, indent=False)

add_para('')

add_para(
    '前端展示层基于HTML5、CSS3和JavaScript构建，采用响应式布局设计，'
    '主要包括影像上传区、检测可视化区和参数结果区三个功能区域。'
    '影像上传区支持拖拽上传和点击选择两种交互方式，并提供文件格式校验和上传进度显示。'
    '检测可视化区将原始影像与检测标注结果以Canvas画布叠加方式并排展示。'
    '参数结果区以结构化表格形式呈现五项临床参数的计算值、参考范围及状态判定。'
    '前端通过Fetch API向后端发送POST请求，以FormData格式封装影像文件及语言偏好，'
    '接收JSON格式的检测结果并动态更新页面内容。'
)

add_para(
    '后端服务层基于FastAPI框架构建，提供四个核心API端点：根路径（GET /）返回'
    '主页面HTML模板；状态接口（GET /api/status）返回模型加载状态及可用设备信息；'
    '模型加载接口（POST /api/load_model）支持按需加载指定类型的检测模型；'
    '检测接口（POST /api/detect）接收上传的X射线影像文件，返回包含关键点坐标、'
    '可视化图像Base64编码及临床参数计算结果的完整JSON响应。'
    '服务层还集成了CORS中间件以支持跨域访问，并实现了异常处理机制，'
    '对模型未加载、无效图像格式及推理异常等情况返回标准化的错误响应。'
)

add_para(
    '模型推理层封装了DRFNet深度学习模型的完整推理流水线：首先将输入图像标准化为'
    '512×512像素的张量，经ResNet50骨干网络提取特征后，通过PAN-FPN金字塔进行'
    '自顶向下和自底向上的多尺度特征融合，最终由热力图解码器输出19通道的'
    '关键点热力图。对每张热力图执行高斯核峰值检测，将最大响应位置映射回原始图像坐标，'
    '得到19个关键点的精确坐标。推理层支持CUDA GPU加速和CPU回退两种模式，'
    '模型权重文件支持从本地路径或远程存储加载。'
)

# ============================================================
#  3 软件功能及使用说明
# ============================================================
add_heading_custom('3 软件功能及使用说明', 1)

# --- 3.1 系统启动 ---
add_heading_custom('3.1 系统启动', 2)

add_para(
    '在完成软件环境的安装与配置后，通过命令行启动系统服务。进入软件安装目录，'
    '执行启动脚本 start.bat（Windows）或在终端中执行以下命令：'
)
add_para('python app.py                # 默认端口8000', indent=False, font_name='Consolas')
add_para('python app.py --port 8080    # 指定端口', indent=False, font_name='Consolas')
add_para('python app.py --device cpu   # CPU模式', indent=False, font_name='Consolas')
add_para(
    '系统启动后，控制台将显示服务地址（默认为 http://localhost:8000）。'
    '此时系统处于就绪状态，模型将在首次检测请求时自动加载。'
    '用户可通过浏览器访问上述地址进入系统主界面。'
)

# --- 3.2 影像上传 ---
add_heading_custom('3.2 影像上传', 2)

add_para(
    '系统主界面顶部为语言切换栏，支持中文与英文的实时切换，点击对应语言按钮即可'
    '切换界面显示语言。语言栏下方为影像上传区域，用户可通过以下两种方式上传'
    '髋关节X射线正位影像：'
)
add_para(
    '（1）拖拽上传：将本地图片文件直接拖拽至上传区域的虚线边框内，松开鼠标后'
    '系统自动识别文件格式并开始上传。'
)
add_para(
    '（2）点击选择：点击上传区域的"选择文件"按钮，在弹出的文件选择对话框中'
    '选取目标影像文件。'
)
add_para(
    '系统支持JPG、PNG、BMP等常见图像格式，上传过程中显示进度条，'
    '上传完成后自动触发关键点检测流程。若上传的文件格式不支持或文件损坏，'
    '系统将弹出错误提示并要求重新选择有效文件。'
)

# --- 3.3 关键点检测与可视化 ---
add_heading_custom('3.3 关键点检测与可视化', 2)

add_para(
    '影像上传成功后，系统自动调用DRFNet深度学习模型对图像进行关键点检测。'
    '检测过程包含以下步骤：首先将上传图像缩放并填充至512×512像素的标准尺寸；'
    '然后将预处理后的图像张量送入ResNet50骨干网络进行多层次特征提取，'
    '经PAN-FPN特征金字塔实现多尺度特征融合；最后通过热力图解码器输出19张关键点热力图，'
    '采用峰值检测算法从每张热力图中提取最大响应位置的亚像素坐标。'
)
add_para(
    '检测完成后，系统在"检测可视化"区域展示处理结果：左侧显示原始上传影像，'
    '右侧显示叠加了关键点标注的可视化影像。标注采用彩色圆点标识各关键点位置，'
    '不同解剖组别使用差异化颜色（骨盆基准点为黄色、左侧髋臼及股骨为橙红色系、'
    '右侧髋臼及股骨为暖青色系），每个关键点旁标注编号（P1-P19）。'
    '同时绘制关键解剖连线，包括骨盆基准线（泪滴连线）、臼杯椭圆拟合线、'
    '股骨干轴线及股骨颈轴线等，便于用户直观验证检测准确度。'
)

# --- 3.4 临床参数计算 ---
add_heading_custom('3.4 临床参数计算', 2)

add_para(
    '系统在完成关键点检测后，自动基于检测到的19个关键点坐标计算五项临床参数，'
    '结果以结构化表格形式展示在可视化区域的右侧。每项参数包含以下信息：'
)

add_para(
    '（1）臼杯外展角（Abduction Angle）：基于髋臼杯开口平面与骨盆水平基准面的夹角计算，'
    '参考范围为30°-50°，用于评估髋臼假体的外展角度是否在安全区间内。'
)
add_para(
    '（2）臼杯前倾角（Anteversion Angle）：基于髋臼杯开口椭圆的短轴与长轴之比的'
    '反三角函数计算，参考范围为5°-25°，用于评估髋臼假体的前倾程度。'
)
add_para(
    '（3）颈干角（Neck-Shaft Angle）：基于股骨头中心-股骨颈中心连线与股骨干轴线的'
    '夹角计算，参考范围为120°-135°，用于评估股骨近端的内翻或外翻畸形。'
)
add_para(
    '（4）股骨偏心距（Femoral Offset）：基于股骨头旋转中心到股骨干解剖轴线的垂直距离计算，'
    '参考范围为30-50 mm，用于评估髋关节外展肌力臂。'
)
add_para(
    '（5）下肢长度差（Leg Length Discrepancy）：基于双侧小转子连线到泪滴连线'
    '的垂直距离差计算，参考范围为-5至5 mm，用于评估术后双下肢不等长程度。'
)

add_para(
    '每项参数的计算结果均与临床参考范围进行比对：若计算值处于正常范围内，'
    '状态栏显示绿色"正常"标识；若计算值超出参考范围上限或下限，'
    '状态栏显示红色"偏高"或"偏低"警示，提醒用户关注该参数的异常情况。'
    '当关键点检测不足以完成某项参数计算时（如相应关键点置信度过低或被遮挡），'
    '系统将显示"数据不足"提示，避免基于不充分数据给出误导性结论。'
)

# --- 3.5 系统状态与调试 ---
add_heading_custom('3.5 系统状态与调试', 2)

add_para(
    '系统提供实时状态监控功能。用户可通过浏览器访问 /api/status 端点查看系统'
    '当前运行状态，包括模型是否已加载、当前使用的计算设备（CUDA GPU或CPU）、'
    '模型类型等基本信息。在后端控制台窗口中，系统会实时输出每次检测请求的'
    '处理时间、检测到的关键点数量及参数计算结果，便于开发者和运维人员进行'
    '性能监控与问题排查。'
)

# ============================================================
#  （额外）与其他系统对照
# ============================================================
add_heading_custom('4 系统特色与适用场景', 1)

add_para(
    '本系统的核心技术创新在于：（1）采用DRFNet热力图回归方法实现关键点的高精度定位，'
    '相较于传统坐标回归方法，热力图方法提供了更鲁棒的定位能力和不确定性估计；'
    '（2）基于解剖学几何关系建立了五项临床参数的标准化计算模型，'
    '所有计算公式均可在骨科临床教材及学术文献中找到对应依据，确保计算结果的'
    '临床可解释性和可信度；（3）全流程自动化，从影像上传到参数计算报告生成'
    '仅需数秒，显著提升髋关节测量的工作效率和标准化程度。'
)

add_para(
    '本系统的主要适用场景包括但不限于：骨科门诊中的髋关节术后快速评估、'
    '髋关节置换术的术前数字化规划、大规模X射线影像数据的批量自动化测量与统计分析、'
    '骨科住院医师及医学生的解剖标志点识别培训、以及临床科研中髋关节相关参数的'
    '大样本流行病学研究等。'
)

# ============================================================
#  保存
# ============================================================
doc.save(OUTPUT_PATH)
print(f'说明书已生成: {OUTPUT_PATH}')
